# ─────────────────────────────────────────────────────────────
# Environment configuration
# Copy this file to .env and adjust values before running.
# NEVER commit .env with real secrets to version control.
# ─────────────────────────────────────────────────────────────

# PostgreSQL
POSTGRES_PASSWORD=postgres

# Airflow
AIRFLOW_FERNET_KEY=81HqDtbqAywKSOumSha3BhWNOdQ26slT6K0YaZeZyPs=
AIRFLOW_SECRET_KEY=inventory_analytics_secret_key_2024
AIRFLOW_USER=admin
AIRFLOW_PASSWORD=admin

# Metabase backend DB (created by init_postgres.sh)
METABASE_DB_PASSWORD=metabase

# Inventory DW (used by DAGs)
INVENTORY_DB_HOST=postgres
INVENTORY_DB_PORT=5432
INVENTORY_DB_NAME=inventory
INVENTORY_DB_USER=inventory
INVENTORY_DB_PASSWORD=inventory
